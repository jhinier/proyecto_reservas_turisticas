@props(['label', 'value'])

<div class="flex justify-between border-b border-gray-100 pb-1">
    <span class="text-gray-500 italic">{{ $label }}</span>
    <span class="font-bold text-gray-800">{{ $value }}</span>
</div>
